package transformer

// extractTests returns the global tests section for the Dalec spec. For simplicity, we return an empty list here.
func extractTests() []string {
	tests := []string{}
	return tests
}
